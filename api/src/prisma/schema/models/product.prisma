model Product {
    id      Int     @id @default(autoincrement())
    name    String  @db.VarChar(50)
    size Sizes
    price Int
    cartItems CartItem[]
    inventory Inventory?

    @@schema("public")
}